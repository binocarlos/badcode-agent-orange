{
  "name": "@platinum/sandbox-image",
  "version": "1.0.0",
  "description": "Platinum agent sandbox image definition: Dockerfile + plugins + workspace-lib. Tests cover the tool plugins; at runtime plugins resolve deps from the core image's /app/node_modules.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.178",
    "@types/node": "^20.14.10",
    "typescript": "^5.5.3",
    "vitest": "^2.1.0",
    "zod": "^4.0.0"
  }
}
